name: sample github action workflow
on: [push]
jobs:
  example-job:
      runs-on: ubuntu-latest
      steps:
          - name: Echo a String
            run: |
                echo "Welcome to github action---with credentials"
                echo "This is second line"
                echo "This is third line"

